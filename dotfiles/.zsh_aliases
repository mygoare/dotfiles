alias grep='grep --color=auto'
alias pg='ps aux | grep'

alias sshu='ssh mygoare@172.16.85.129'
alias sshvps='ssh mygoare@173.252.197.181'

#C-r
bindkey "^R" history-incremental-search-backward

# tmux lose vim colorscheme
alias tmux="tmux -2"

alias vim="vim -p"

# fix terminal 中文乱码
export LANG='zh_CN.UTF-8'

alias ...="cd ../../"
alias ....="cd ../../../"
alias ~="cd ~"
alias p="cd ~/Projects"
alias dl="cd ~/Downloads"
alias dt="cd ~/Desktop"
alias dc="cd ~/Documents"
alias www="cd /Library/WebServer/Documents"

alias lsa='ls -lahtr'
alias l='ls -lA1tr'
alias ll='ls -ltr'
alias la='ls -lAtr'

alias v='vim'

#git
alias gcaa='git commit -a --amend'

SAVEHIST=1000

#alias yuicompress='java -jar ~/Projects/Kit/yuicompressor/build/yuicompressor-2.4.8pre.jar'
alias htmlcompress='java -jar /Users/mygoare/Project/Kit/htmlcompressor-1.5.3.jar'


#SET PATH
PATH=/usr/local/bin:/usr/local/mysql/bin:$HOME/.rvm/bin:$PATH

#path about ruby, rails, rvm, nodejs, nvm etc.
PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

[[ -s "$HOME/.nvm/nvm.sh" ]] && . "$HOME/.nvm/nvm.sh" # This loads NVM

export NODE_PATH=$NVM_DIR/$(nvm_ls current)/lib/node_modules # export $NODE_PATH
